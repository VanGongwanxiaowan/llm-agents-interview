# 02 · 上下文工程（全书最关键一章）

> 来源：《深入理解 AI Agent》第 2 章
> 上下文 = Agent 的"眼睛"，决定 Agent 能力上限。本章主线：**显式管理信息**。

---

## 2.1 上下文：决定 Agent 能力上限的关键

- **核心论点**：模型智力只是基础，**上下文质量才是 Agent 能力的真正关键**。一个中等模型配精心组织的上下文，往往胜过顶级模型在信息匮乏下的盲目摸索。
- 类比：天才工程师加入团队，但不知道产品架构、业务逻辑、技术债务、团队规范，也难以发挥价值。
- 三类最低信息需求（以 Coding Agent 修 bug 为例）：
  1. **实时代码上下文**：目录结构、模块职责、核心数据结构、代码规范。
  2. **流程规范**：Git 分支策略、提交规范、代码审查流程、CI/CD 要求。
  3. **环境信息**：开发环境配置、测试库地址、部署方式、密钥管理。
- **进入上下文的是对环境的观察/描述/配置，而不是环境本身**。
- **组织问题**：团队关键知识是隐性的（架构决策只老员工记得、业务规则口口相传）→ 团队信息黑洞，再好的 Agent 也无计可施。**构建 AI 原生团队首先是一场文档化运动**。
- ReAct 论文定义：Agent 下一步行动取决于截至当前的完整交互上下文 c_t = (o1, a1, ..., o_{t-1}, a_{t-1}, o_t)。
- 模型 API 无状态，每次调用必须由 Agent 框架重新构造足够上下文；可以摘要压缩，但不能悄悄丢掉决定下一步所需的信息。

---

## 2.2 Agent 如何调用大模型：API 的上下文结构

### 2.2.1 消息的四种角色 + tools 字段

| 角色 | 含义 | 说明 |
|------|------|------|
| **system** | 系统提示词 | 开发者写，定义身份/规则/约束，最高优先级，通常只有一条在最前 |
| **user** | 用户消息 | 终端用户输入 |
| **assistant** | 助手消息 | 模型之前的回复（文本 + 工具调用请求） |
| **tool** | 工具结果 | 框架执行工具后送回，通过 tool_call_id 关联对应调用 |

- **tools 字段**是请求顶层独立字段（不是消息角色），声明可用工具及参数 schema。
- 这恰好覆盖第一章"上下文五个组成部分"。

### 2.2.2 核心要点

- **每次调用都是无状态的**：所有模型需要的信息必须在 messages 列表完整提供。
- 模型只负责**决策**（调什么工具、传什么参），Agent 框架负责**执行**（实际调 API、跑代码）。
- **并行 vs 串行**：工具间无数据依赖可并行；后一工具参数依赖前一工具结果则必须串行（模型后续轮再发起调用）。
- 多轮交互三个关键细节：
  1. 每轮都包含全部历史（system + user + 之前 assistant + 新增 tool 结果）。
  2. 之前的 assistant 消息原样放回（让模型"看到"自己之前的决策）。
  3. tool 消息通过 tool_call_id 关联对应调用。

### 2.2.3 代码实现 Agent 核心循环（关键）

```python
from openai import OpenAI
client = OpenAI()
tools = [ ... ]  # 工具定义（name, description, parameters）

messages = [
    {"role": "system", "content": "You are a helpful assistant..."},
    {"role": "user", "content": "What's the current time and weather in Vancouver?"},
]

while True:  # 生产代码必须加 max_iterations 上限
    response = client.chat.completions.create(model="...", messages=messages, tools=tools)
    assistant_message = response.choices[0].message
    messages.append(assistant_message)  # 追加模型回复
    if not assistant_message.tool_calls:  # 无工具调用 → 最终回复，退出
        print(assistant_message.content)
        break
    for tool_call in assistant_message.tool_calls:  # 执行每个工具
        result = execute_tool(tool_call.function.name, tool_call.function.arguments)
        messages.append({"role": "tool", "tool_call_id": tool_call.id, "content": result})
```

> **Agent 框架的核心工作 = 管理 messages 列表**：在合适时机追加消息，把整个列表送模型。后续所有上下文工程技术本质上都在优化这个列表的内容和结构。

### 2.2.4 上下文构造决策（最小骨架伪代码）

```
stable_prefix = system_message
stable_tools = core_tool_schemas
trajectory = load_message_history(session)
status_message = make_status_message(derive_current_state(trajectory))
if estimated_tokens(stable_prefix, trajectory, status_message) > budget:
    trajectory = compress_old_evidence(trajectory, preserve=[decisions, constraints, failures, citations])
request.messages = [stable_prefix] + trajectory + [status_message]
request.tools = stable_tools
response = call_model(request)
```

> 原则：系统提示词和核心工具定义尽量稳定；旧工具输出只在接近预算时成批压缩；当前状态放在轨迹尾部。

---

## 2.3 KV Cache 友好的上下文设计

### 2.3.1 三条核心结论（必须记住，可跳过原理）

1. **系统提示词和工具定义一旦确定就不要改**——任何改动（哪怕多一个空格）都可能改变 token 序列，使首个不同 token 及其后缓存失效；改动越靠前，影响越大。
2. **动态信息永远追加到末尾**——时间戳、用户状态等变化内容作为新消息追加，而非修改已有系统提示词。
3. **使用标准 API 格式，不要自行拼接消息**——结构化消息会被 Chat Template 翻译成训练时见过的固定 token 序列；自行拼接会偏离训练格式，削弱多步思考能力。

### 2.3.2 注意力机制直觉（Q/K/V）

- Query（查询）= 当前词发出的"搜索请求"；Key（键）= 每个词的"标签"；Value（值）= 每个词的"内容"。
- 计算三步：当前词生成 Query → Query 与各 Key 点积打分 → softmax 归一化为权重 → 对 Value 加权求和。
- **因果三角**：每个词只能看到自己和前面的词。
- **注意力储存池（Attention Sink）**：序列第一个 token 吸收异常高注意力（有时 >70%），因为 softmax 强制所有权重和为 1，模型把"剩余权重"倒进开头固定位置（系统性现象，非缺陷）。
- **位置偏好（Position Bias / Lost in the Middle）**：模型对上下文开头和结尾注意力更高，中间易被忽视 → **把最关键信息放开头或结尾**。

### 2.3.3 Chat Template（贯穿全书的基础机制）

- 把结构化 API 消息转换为模型能理解的线性 token 流（如 `<|im_start|>system ... <|im_end|>`）。
- 不同模型家族（Qwen/Llama/Gemma）用不同"信封格式"，API 服务端自动转换，开发者通常不用手动处理。
- 两个实用价值：
  1. **解释为什么必须用标准 API 格式**：自行拼接会把工具结果误识别为新用户查询，触发思维链清理机制（Qwen3 检测到新 user 查询会清理之前 `<think>` 思考），破坏多步思考连贯性。
  2. **解释 KV Cache 为什么对前缀敏感**：system 消息和工具定义转为固定 token 序列放最前，缓存后跨请求复用；前缀任一 token 变化，其后缓存失效。

**历史思维链（CoT）处理策略（重要、且各家不同）**：
- DeepSeek R1 时代：剥离全部历史思考（多轮只回传 content 不回传 reasoning_content），因为 R1 训练时历史 CoT 从不在输入里，塞回是分布外输入；但这对 Agent 有缺陷——中间思考承载"为什么调这个工具、排除了哪些假设"。
- DeepSeek V4：**彻底反转**，强制要求回传每轮 assistant 消息（含 tool_calls）的 reasoning_content，否则报错；Kimi K2、GLM-5 同。
- Claude：客户端在工具调用循环中把 thinking block（带签名校验）原样回传，新用户输入后服务端忽略之前 thinking block。
- **结论**：使用前查阅对应模型最新文档。

### 2.3.4 KV Cache 原理

- 无缓存时，每生成新 token 都要重算前面所有 token 的 K、V，prefill 注意力计算量随上下文长度**平方级**增长。
- 有缓存时，历史 token 的 K/V 算过一次就缓存，新 token 只需算自己的 K/V + 遍历全部缓存 K/V（注意力计算仍随长度**线性**增长）。
- **为什么改前缀导致后面失效**：多层 Transformer 串联，第 k 个 token 变化，k 之前状态不受影响，但从 k 起逐层受影响，缓存只能保留到首个不同 token 之前。
- KV Cache 省的是历史 token 的 K/V 投影重算；长上下文解码慢、KV Cache 显存与带宽是推理瓶颈。

### 2.3.5 常见错误上下文管理模式（实验 2-3）

| 错误模式 | 问题 |
|----------|------|
| **动态系统提示词**（时间戳注入 system） | 破坏缓存，首 token 延迟 0.5s→3-5s，成本翻倍 |
| **动态用户配置**（余额、配额嵌入） | 破坏缓存；应用专门状态管理机制 |
| **工具定义动态排序** | 工具定义占大量 token，改顺序缓存全失效；固定顺序对选择能力几乎无影响 |
| **滑动窗口对话历史** | ①破坏前缀一致性→缓存失效；②丢失关键工具结果→Agent 反复执行相同调用（"忘记"已获得结果） |
| **文本格式化**（role-content 转纯文本） | 偏离训练格式，模型需额外注意力推断角色边界→重复执行、忽略工具结果、该调用工具却生成文本 |

> 补充：模型提供商为标准接口做了大量优化，偏离标准格式是在给自己挖坑。

### 2.3.6 KV Cache vs Prompt Cache（两个层级）

| | KV Cache | Prompt Cache |
|--|----------|--------------|
| 层级 | 模型内部机制 | 推理引擎优化 |
| 作用 | 单次推理内缓存已算 token 键值对 | 多次 API 请求间缓存相同前缀 |
| 目标 | 加速单次请求内 token 生成 | 减少跨请求重复计算成本 |

- Prompt Cache：API 服务商对请求前缀匹配，相同前缀直接复用已算 KV Cache；读取成本约为首次计算的 1/10（Anthropic、DeepSeek、GPT-5）。

### 2.3.7 缓存作为架构约束（Claude Code 实践）

- **当 Prompt Cache 经济效益显著时，缓存一致性反过来主导架构选择**：
  1. **提示词结构由缓存边界决定**：system prompt 被缓存边界标记分两部分——标记前可跨用户/跨会话全局缓存，标记后含用户/会话特定信息。**所有动态元素放边界之后**（N 个二值条件会产生 2^N 种缓存键组合）。
  2. **子 Agent 必须与父 Agent 字节级对齐**：继承父上下文时，提示词/工具定义/模型配置/消息前缀/思考配置必须逐字节匹配，才能命中 Prompt Cache。
  3. **工具结果替换字符串在首次出现时冻结**：摘要预览的替换字符串持久化保存，会话重启也用相同字符串，保证恢复后消息序列与缓存字节流一致。
- **启示**：缓存经济性不是事后优化，而是前置约束，越早纳入架构设计代价越小。

### 2.3.8 前沿延伸：可编辑、可组合的 KV Cache（选读）

- 反直觉观察：prefill 阶段模型在"做笔记"——读到字段时把"这个字段意味着什么"的结论写进下游每层 KV 状态，字段自身 token 的 KV 对最终决策贡献往往 <1%。
- 由此打开两种操作：
  - **编辑（Editing）**：改字段后，若有显式 CoT，可让改动顺着已缓存思考传播，用 ~1% 算力得到与整段重算一致的结果（无 CoT 则孤立改字段被忽略）。
  - **组合（Composition）**：把预先算好的"技能"缓存通过 RoPE 旋转位置编码挪到新位置，直接拼接，从 O(L²) 重算降到 O(L) 拼接。
- 效果：vLLM 实现首 token 延迟（p90）降低数十到数百倍，前缀缓存命中率 ~98.5%。
- **对 Agent 意义**：长上下文（换工具、更新记忆字段、注入新状态）也许不必每轮推倒重来，指向"上下文可变但缓存收益还在"的可能。**仍属研究阶段**，前面三条实践结论仍是生产默认原则。

---

## 2.4 提示工程：优化系统提示词

> 检验标准：**如果一个聪明的新员工读完系统提示词还不知道该怎么做，Agent 也一样不知道。**

### 2.4.1 语气与风格（人格）

- 可明确要求"简洁回答不超 4 行"、无法完成时"控制在 1-2 句话，不解释为什么不能做"。
- 大写（"NEVER do X"）比"Please avoid"更能引起注意，但过度使用会稀释效果，保留给真正关键约束。

### 2.4.2 结构化提示（格式）

- 现代 LLM 对结构化输入显著敏感（训练数据含大量结构化内容）。
- **XML**：标签名携带语义（`<working_directory>` 立即告诉模型这是工作目录）。**Markdown**：轻量可读，适合层次化组织。
- **双层结构**：XML 负责机器可解析的精确语义，Markdown 负责人机共读的组织逻辑。

### 2.4.3 流程驱动 vs 规则堆砌（组织方式）

- 上百条零散规则无流程图无优先级 → 模型困惑（多条规则冲突、未覆盖情况如何办）。
- **流程驱动（SOP）**：给清晰标准操作流程（如 File Processing SOP：Validation → Classification → Preprocessing → Execution → Verification），模型任何时刻都知道自己处于哪个阶段、目标、下一步。

### 2.4.4 业务规则细化（内容，最重要最易忽视）

- **这是产品设计问题，需要产品经理深度参与**。工程师不应擅自决定业务逻辑。
- 案例（账单协商 Agent 计费模式）：模糊规则（"根据任务选计费类型"）导致行为极不稳定——"退衣服"是省钱还是取回本属于他的钱？"取消订阅"算省钱吗？
- **必须细化到可执行**：按提成计费仅限"谈判降低现有账单"；退款和取消绝不能按提成——提示词明确写 "NEVER use percentage-based for refunds and cancellations. Use fixed_fee instead."
- 成功率估算、金额计算都要写死（通话每分钟 $0.05、四舍五入到整数美元、"节省"只基于现有账单，不能把避免未来涨价算进去）。
- **哲学**：LLM 优势是遵循复杂指令、从长上下文提取信息，**不应在业务规则制定上被赋予过多自由裁量权**。用清晰操作框架解放认知资源，让模型专注真正需要思考的部分。

### 2.4.5 Few-shot 示例

- 何时用：期望输出难用规则精确描述时（特定风格文案、报告格式、语气分寸），2-3 个高质量输入输出示例胜过等量篇幅抽象规则。
- 何时不用：模型本就擅长、规则易说清的任务，示例只是浪费 token。
- **放哪里**：放 system prompt（静态前缀，所有请求生效）或伪造 user/assistant 消息放首轮对话（按会话类型选示例集）。
- **对 KV Cache 影响**：示例在靠前区域，一旦确定应保持字节级稳定；按请求动态检索示例 = 每次改写前缀 = 缓存持续失效。生产系统为每类任务准备固定示例集。
- 数量：2-3 个精选覆盖边界的示例胜过 10 个大同小异的（后者占用上下文还稀释注意力）。

### 2.4.6 工具定义的设计

- 工具定义质量直接决定工具使用准确性。好描述让从未用过的人立即正确使用、避免常见错误。
- 从 Claude Code 观察：精心设计使用边界（"NEVER invoke grep as Bash command"）、具体示例（timezone 值）、性能提示（"Batch your tool calls"）、工具协作关系（"Use Read at least once before editing"）。详见第四章。

**工具定义也在向渐进式披露演进（API 层原生能力，2026 起）**：
- OpenAI Responses API：`tool_search` 工具 + `defer_loading: true`，模型通过 tool_search_call → tool_search_output 按需加载完整 schema。
- Anthropic：Tool Search（tool_reference blocks），Claude Code 对 MCP 工具默认延迟加载（会话启动只注入名称和说明）。
- Codex CLI：tool_search（BM25 检索）默认开启。
- **共同点**：静态前缀只保留工具名称和简述，完整 schema 按需请求后**追加到上下文末尾**（成为轨迹一部分）。

**为什么追加到末尾不破坏缓存**：因果注意力决定每个 token 的 KV 只依赖之前 token，末尾追加不改变任何已缓存 token 的 KV。注意"追加到末尾"只发生在工具被发现那一轮，此后 schema 块固定在轨迹原位置。

**约束**：模型必须在训练中见过"工具定义出现在对话中间"这种模式——目前只有较新模型（GPT-5.4+、Claude 4.5+）支持。

### 2.4.7 提示注入（Prompt Injection）

- **本质**：攻击者通过 Agent 处理的外部内容（网页、邮件、文档）把伪装成系统指令的文本混入上下文，劫持行为。
- **比普通聊天机器人更危险**：Agent 有工具调用能力，注入可能导致删除文件、发邮件、泄露隐私等不可逆操作。
- **攻击面随能力增长**：每个感知工具（网页阅读、文档解析、邮件处理）都是注入入口。攻击者可在网页不可见元素、PDF 元数据、图片 EXIF 嵌入指令。
- **上下文层防御（只能降风险，不能保证）**：
  1. **来源标记**：外部内容注入前用 `<external_content source="webpage">...</external_content>` 包裹标注。
  2. **结构化角色**：严格用 Chat Template 角色体系传递信息，让模型按训练优先级区分可信指令与外部数据（这也是"不要自行拼接消息"的又一理由）。
  3. **输入清洗**：过滤可疑模式（"忽略之前指令"等），只能作辅助（易被措辞变体绕过）。
- **Skill 是新的注入面**：Skill 本质是"把外部内容当指令加载"的制度化形式，第三方 Skill 藏恶意指令比网页隐藏文本更直接，安装前必须审查。状态栏同理（状态摘要若来自可被外部污染的数据源，信任被反向利用）。

---

## 2.5 动态提示词与 Agent Skills

### 2.5.1 动机

系统提示词膨胀带来两个问题：① 浪费 token（大部分内容与当前任务无关）；② 注意力被稀释（"上下文腐化"）。→ 从静态提示工程演进到动态提示词：按需加载。

### 2.5.2 Skills：领域能力的可组合单元（三层渐进式披露）

| 层 | 内容 | 加载时机 | Token |
|----|------|----------|-------|
| 第一层 元数据 | SKILL.md 的 YAML frontmatter（name + description） | 启动时加载 | ~300 tokens |
| 第二层 核心流程 | SKILL.md 完整指令（核心 workflow） | 判断需要时加载 | ~2K tokens |
| 第三层 细则 | 子文档（html2pptx.md、reference.md、scripts/*.py） | 选择性深入 | 按需 |

- **description 是路由决策关键**：应足够短（控制常驻 token），写法像**路由条件**而非功能介绍——明确"何时使用/何时不使用"边界 + 典型反例。描述太宽泛（"help with backend"）路由失准。**"何时该用我"比"我能做什么"重要得多**。

### 2.5.3 如何编写可用的 Skill（宝玉《图解 Skill》）

四部分：
1. **角色与读者**：服务谁、面向什么任务、输出标准。
2. **核心原则**：只保留 3-5 条最重要判断，配正例反例。
3. **禁止清单**：高频错误、越权动作、易误解表达，写清合法例外。
4. **参考资料**：术语表、模板、范文、子文档。

- 规则尽量写成"**作用域 + 动作 + 例外 + 验证方式**"，避免堆成越来越长的禁用词表。
- 写作型 Skill：从 3-5 篇满意原创开始，让 Agent 归纳生成二十行初版，再处理真实任务，作者逐句改稿，把反复出现的改动整理回 Skill（保留正例、反例、适用范围）。
- Skill 可捆绑可执行代码工具和模板文件。
- **价值**：为领域知识积累提供可持续路径；模块化可独立开发/测试/版本控制/分享（类似 pip/npm 生态）。

### 2.5.4 Skills 在上下文中的位置

- **规范规定的是加载时序而非消息角色**：目录先于正文可发现，正文选中后按需加载；具体消息角色、包装方式由 Harness 决定。
- Claude Code：目录作为运行时上下文消息（system 层），完整指令在调用位置作为 user message 注入。
- OpenAI Codex：每轮重新渲染 Skills catalog 作为 developer 上下文片段，选中 Skill 正文以带 `<skill>` 标记的 user 片段注入。
- 不同 Harness 实现不同，但都遵循"**少量目录常驻、完整正文按需加载**"。

### 2.5.5 Skills 与工具的关系

- Skills 机制对 KV Cache 友好：专用工具定义放 system 会 token 膨胀干扰注意力；Skill + 通用执行器模式下工具数量很少（第 5 章仅需 7 个核心工具），Skill 内容按需加载不影响已缓存前缀。
- 两种形态对比选择框架见第 4 章；"经验写成知识/指令/程序/参数"见第 9 章。

---

## 2.6 Agent 状态栏（Agent Status Bar）

### 2.6.1 定义

- Agent 框架把动态信息（任务进度、环境变化、工具调用计数）整理成**结构化摘要注入上下文末尾**的机制。
- 类比手机屏幕顶部状态栏（时间/电量/信号）：不是主内容，但随时可瞥一眼掌握状态。

### 2.6.2 理论基础

- **上下文学习更像检索而非推理**：模型擅长从已有内容"查找"，不擅长主动"归纳统计"。
- "上下文窗口是一台只有一半的检索引擎"：检索的一半很强（注意力能从成千上万 token 捞相关记录），但缺"提炼层"——不会自动数一遍、建索引、就地总结结论。
- 例子：打了 3 次电话，模型数不清又打第 4 次。根源是"打了几次"的知识没被提炼，而是以原始记录分散在 KV Cache 向量中。
- **效果（实验 2-8）**：提供状态栏后，小模型准确率接近前沿大模型；每次迭代思考 token 量/延迟/花费降低约一个数量级；带状态栏后每次查询思考量基本恒定（不带则随上下文变长持续增长）。
- **注意力衰减**：长上下文下，早期目标与关键约束被后续大量工具结果淹没，模型过度关注最近内容。

### 2.6.3 状态栏构成

1. **任务规划（TODO 列表）**：分解为清晰步骤放轨迹末尾，提醒进展与后续目标，防止过度关注局部子任务忘记原始诉求。
2. **事件侧信道信息（Side-channel Information）**：为每个事件附加元数据（精确时间、地理位置、距上次回复间隔），帮模型理解时序关系。
3. **环境当前状态观察摘要**：动态环境信息（系统时间、工作目录）、异常操作提醒（"工具已重复调用 N 次"）、隐式状态到显式观察的转换。

### 2.6.4 状态栏在上下文的位置

- **在 API 层面作为一条 user 角色消息插入到上下文末尾**（不是修改 system，否则破坏缓存）。
- 澄清：这里 user 角色只是 API 协议技术选择，不等同于"来自终端用户的输入"——Harness 借用 user 消息槽位注入框架自动生成的系统状态信息。
- 用 `<agent_status>` 标签包裹以便模型识别特殊性质。
- 紧邻生成位置 → 获得最高注意力权重；追加而非修改 → 不影响已缓存内容。

### 2.6.5 状态更新的两种实现与缓存代价

| 实现 | 做法 | 优点 | 代价 |
|------|------|------|------|
| **实现一：每轮替换** | 移除上一轮状态消息，末尾追加最新 | 上下文只有一份状态，永远最新 | 移除旧状态使其后缓存失效（但失效范围只覆盖上次注入后的短后缀） |
| **实现二：持久追加** | 状态消息永久留轨迹，每轮只追加新状态（Claude Code `<system-reminder>` 方式） | 缓存完全友好（只追加不修改） | 陈旧状态累积占 token，要求模型"关注最新一条" |

**取舍**：状态小、更新间消息多、会话受控 → 选实现二；状态大、更新频繁、轨迹很长 → 选实现一。

**粗略估算模型**：设每条状态 S token，两次更新间新增后缀 R token，更新 N 次，缓存输入单价为普通输入 α 倍。
- C_替换 ≈ (N−1)(1−α)R
- C_追加 ≈ αSN(N−1)/2
- 当 αSN/2 < (1−α)R 倾向实现二，否则倾向实现一。

### 2.6.6 五种好用的状态栏技术（实验 2-9）

1. **时间戳跟踪**：`[2025-09-14 10:30:45]` 前缀加到用户消息和工具响应（**不是 system，否则破坏 KV Cache**），支持时间模拟。
2. **工具调用计数器**：全局字典记录每工具调用次数，标注 "Tool call #3 for read_file"。触发模式识别（第一次失败查路径、第二次列目录、第三次放弃找替代）；实现隐式成本感知。
3. **TODO 列表管理**：提供 rewrite_todo_list / update_todo_status 工具，每项含唯一 ID、内容、状态（pending/in_progress/completed/cancelled）、时间戳。**启用 TODO 的 Agent 平均 15 次迭代完成，禁用需 21 次且常遗漏子任务**。
4. **详细错误信息**：四层（错误类型描述、完整参数 JSON、调用栈、修复建议）。**成功率从 60% 提升到 95%**（从盲目重试变为针对性分析）。
5. **系统状态感知**：注入当前时间、工作目录、OS 类型、Shell、Python 版本（OS 信息使 Agent 做平台相关决策）。

> **涌现效应**：技术组合产生超出预期的效果。

**两点维护注意**：
1. **状态栏尽量用代码维护**；实在用 LLM 也要逐条抽取、代码汇总，绝不让它一次性批量统计（模型几乎无条件相信状态栏，且数量统计易错；也意味着状态栏投毒风险要认真对待）。
2. **谨慎删除原始上下文**：状态栏是有损投影，只提前算了"预想会被问到"的维度。够用可删原始记录省 token，但只要一个问题涉及未计算维度，仅保留状态栏会导致准确率断崖式下降。

---

## 2.7 上下文压缩策略

### 2.7.1 三个压缩动机（不只是长度）

1. **解决长度/成本约束**：窗口有限、token 越多成本越高、延迟上升。
2. **提升思考质量**：总结后的知识比原始形式更利于模型使用；先结构化总结成"目前已知 A、B，还缺 C"，后续直接使用精炼表示。
3. **缓解上下文焦虑（Context Anxiety）**：模型认为窗口即将耗尽时可能提前收尾；在未接近耗尽时提前压缩可提升决策质量。

### 2.7.2 内部机制：检索而非推理

- 例子：100 个笼子 90 黑猫 10 白猫，"黑猫白猫各多少"——不启用 CoT 很难直接答对（查找是强项，统计归纳是思考而非检索）；启用 CoT 能数对但每问一次从头数一遍。
- **压缩的第二价值**：把需要思考才能得到的结论，变成可直接检索的知识。
- **上下文腐化（Context Rot）** vs **上下文溢出**：溢出是"装不下"，腐化是"装得下但找不到"（更隐蔽，Agent 表面正常但决策质量悄然下降）。
- **设计原则**：与其期望模型从冗长上下文自动学习，不如**主动显式进行知识提炼**（用专门 LLM 调用做总结，产生高密度知识表示）。

### 2.7.3 压缩与 KV Cache 的关系（看似矛盾实则互补）

- 压缩发生在**两次 API 调用之间**，由框架对消息列表预处理：
  1. System Prompt + Tool Definitions 永远不动。
  2. 压缩对象是对话历史中的 tool results（替换位置之后缓存失效，之前仍有效）。
  3. 有意识权衡：不压缩→窗口溢出任务失败；压缩→损部分缓存但长度可控、密度更高。
- **压缩频次**：最好接近阈值时**批量压缩**，而非每轮都压。

### 2.7.4 六种压缩策略对比（实验 2-10）

| 策略 | Token 用量 | 压缩率 | 迭代次数 | 结果 |
|------|-----------|--------|----------|------|
| 无压缩 | 166,043 | 102.1% | 5 | ✗ 失败 |
| 个体摘要 | 276,608 | 10.9% | 12 | ✓ 成功 |
| 组合摘要 | 93,449 | 4.3% | 10 | ✓ 成功 |
| **上下文感知** | **40,157** | **3.0%** | **7** | ✓ 成功 |
| 感知+引用 | 222,992 | 4.1% | 10 | ✓ 成功 |
| 自适应窗口 | 174,601 | 102.4% | 7 | ✓ 成功 |

> 压缩率 = 压缩后体积/原文体积，数值越小压得越狠。

- **上下文感知压缩最佳**：将查询意图 + 已积累信息纳入压缩决策（"Given the search query: {query}" + "Current context: {context}"），相比无压缩省 76% token，并列最少迭代。
- 个体摘要：信息碎片化（多页重复描述同一事件）。组合摘要：输入超长时截断丢末尾信息。两者共同缺陷是缺语义理解。
- 带引用：语义压缩（有损）+ 保留源链接（无损索引），可回溯。
- 自适应窗口：阈值触发（超 80% 窗口才激活）+ 批量压缩 + 防重复保护（[COMPRESSED] 标记）。

### 2.7.5 生产级分层压缩机制（Claude Code，五层）

1. **工具结果预算控制**：大体积输出存磁盘，模型只看摘要预览；替换决策一旦做出就冻结（保缓存一致）。
2. **噪声直接删除**：低价值内容直接移除不做摘要（对噪声做摘要浪费 token）。
3. **API 层微压缩**：通过 API 上下文编辑能力让服务端从前缀移除指定工具结果；适合即将溢出时使用（反正要付缓存重建代价），不宜频繁触发。
4. **归档式摘要**：逐轮结构化摘要（像 git log 保留每轮独立记录，而非 git squash 合并）。
5. **全量压缩**（最后手段）：LLM 驱动，分两阶段（先压会话记忆，不行再全量）；配连续失败熔断器（大量会话困在反复压缩失败循环，熔断避免持续烧钱）。

### 2.7.6 压缩策略设计原则（四条）

1. **信息价值非均匀分布**：关键决策点（人员名单）> 支撑证据（新闻细节）> 冗余噪声（导航栏、页脚广告）。
2. **语义完整性**："Sutskever 于 2024 年 5 月离开 OpenAI"不能压成"Sutskever 离开"（时间、公司名不可丢）。
3. **任务相关性**：同样内容不同任务应产生不同压缩结果——检索类保留广度、分析类保留深度、创作类保留灵感触发点；理想 Agent 按任务类型自适应选择。
4. **压缩即理解**：有效压缩需深层语义理解，压缩模块本身要接近主模型能力（"模型调用模型"递归架构）；好处是显式压缩结果可审查、可跨会话复用。

- 压缩最易丢失：早期架构决策、约束背后理由、失败路径 → Agent 需定期把进展记录到文档（而非零散放执行历史），没有文档化习惯就用 prompt/skill 提醒。

### 2.7.7 隔离优于压缩：子 Agent 上下文隔离

- 思路：让大体积中间信息**根本不进入主上下文**——主 Agent 把"代码库大范围搜索"委派给独立子 Agent，子 Agent 在自己上下文完成探索，只回传几百 token 结论摘要。
- 对比：主 Agent 亲自搜索会把数万 token 原始代码纳入主上下文（找到目标后沦为噪声）；委派子 Agent 主上下文只增两条消息（任务描述 + 结论），中间数万 token 随子 Agent 上下文丢弃。
- **隔离 vs 压缩**：压缩是有损、需额外 LLM 调用的事后补救；隔离让噪声从开始就与主上下文绝缘，主 Agent 的 KV Cache 前缀完全不受影响。
- 代价：子 Agent 看不到主 Agent 完整上下文，任务描述必须自包含、目标明确。
- 生产实现：Claude Code 的 Task 工具、Deep Research 检索子 Agent。详见第 4 章（协作工具）、第 10 章（多 Agent 上下文架构）。

---

## 2.8 本章小结

上下文工程主线是**显式管理信息**：
- API 消息结构定义骨架；
- 稳定前缀提高 KV Cache 命中；
- Prompt、Skills、状态栏分别承载**规则、按需知识、当前状态**；
- 压缩在保留**决策、约束、失败、来源**的前提下提高历史信息密度。
